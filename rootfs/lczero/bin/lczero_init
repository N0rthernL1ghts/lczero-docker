#!/usr/bin/env bash
SCRIPT_PATH="$(
    cd "$(dirname "$0")" || exit 1
    pwd -P
)"

cd "${SCRIPT_PATH}" || exit 1

main() {
    /lczero/bin/lc0 --config=/lczero/resources/lc0.config
    return $?
}

main "${@}"
exit $?
